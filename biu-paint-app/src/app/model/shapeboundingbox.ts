export class ShapeBoundingBox {
    left: number;
    top: number;
    height: number;
    width: number;

    constructor(left: number, top: number, height: number, width: number) {
        this.left = left;
        this.top = top;
        this.height = height;
        this.width = width;
    }
}